// Single source of truth for "who is logged in" in this prototype.
// A real product would derive this from auth; here it's fixed so the demo
// always opens straight into María's account.
export const USER_ID = "maria";
